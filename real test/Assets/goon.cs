using UnityEngine;

public class goon : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
     print("karel is een adlin abuser not sigma >:(");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
